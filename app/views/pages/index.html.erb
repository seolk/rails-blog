<h1>Pages#index</h1>

<% @pages.each do |page| %>
  <%= page.title %>
  <%= link_to 'show page', page_path(page) %>
  <%= link_to 'edit page', edit_page_path(page) %>
  <%= link_to 'Delete page', page, method: :delete %>
<% end %>


<%= link_to 'Add a new Page', new_page_path %>